@model IEnumerable<HotelApp.Models.RoomAmenities>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoomId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmenitiesId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AmenitiesId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomAmenitiesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomAmenitiesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomAmenitiesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
